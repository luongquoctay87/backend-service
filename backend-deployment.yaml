apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  selector:
    matchLabels:
      app: backends
  template:
    metadata:
      labels:
        app: backends
    spec:
      containers:
        - name: backends
          image: backend:1.0
          imagePullPolicy: Never
          resources:
            requests:
              memory: "300Mi"
            limits:
              memory: "500Mi"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  type: ClusterIP
  selector:
    app: backends
  ports:
    - name: http
      protocol: "TCP"
      port: 80
      targetPort: 8080
